# CSV Data Cleaner — Abhängigkeiten
# Installieren mit: pip install -r requirements.txt

# Datenverarbeitung
pandas>=2.0.0

# Testing (optional, nur für Entwicklung)
pytest>=7.4.0
pytest-cov>=4.1.0

# GUI: tkinter ist Teil der Python-Standardbibliothek.
# Falls es auf Linux fehlt, installieren mit:
#   sudo apt-get install python3-tk
